import { ScrollText } from 'lucide-react';
import { PlaceholderPage } from '../../components/PlaceholderPage';

export function SystemLogs() {
  return (
    <PlaceholderPage
      title="System Logs"
      description="View system-wide logs and events."
      icon={ScrollText}
    />
  );
}
