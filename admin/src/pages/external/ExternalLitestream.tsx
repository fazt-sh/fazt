import { Database } from 'lucide-react';
import { PlaceholderPage } from '../../components/PlaceholderPage';

export function ExternalLitestream() {
  return (
    <PlaceholderPage
      title="Litestream"
      description="Manage Litestream database replication."
      icon={Database}
    />
  );
}
